import SwiftUI

extension Color {
    static let prismBackground = Color("prismBackground")
    static let prismSurface = Color("prismSurface")
    static let prismSurfaceAlt = Color("prismSurfaceAlt")
    static let prismBorder = Color("prismBorder")
    static let prismBorderHover = Color("prismBorderHover")
    static let prismTextPrimary = Color("prismTextPrimary")
    static let prismTextSecondary = Color("prismTextSecondary")
    static let prismTextTertiary = Color("prismTextTertiary")
    static let prismAccent = Color("AccentColor")
    static let prismAccentDim = Color("AccentColor").opacity(0.12)
    static let prismAccentWarm = Color("prismAccentWarm")
    static let prismAccentWarmDim = Color("prismAccentWarm").opacity(0.12)
    static let prismSuccess = Color("prismSuccess")
    static let prismWarning = Color("prismWarning")
    static let prismError = Color("prismError")
}
